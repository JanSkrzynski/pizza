<h1>All Orders</h1>

<% orders.forEach(order => { %>
<div class="order">
  <strong>Order #<%= order.id %></strong> - <%= new
  Date(order.created_at).toLocaleString() %>
  <p>Status: <%= order.status %></p>
</div>
<% }) %>
